---
name: mobbin-design-research
description: Use when the user asks for mobbin design research ingestion or gives a prompt that matches the repeated workflow evidence from Relay conversation history. This skill is a first draft generated from local Codex, Claude, and Relay transcripts; refine after two or three real uses.
---

# Mobbin design research ingestion

## Workflow

1. Restate the concrete task and identify the active project. Common projects from history: mobbin-library, relay.
2. Gather local context before changing anything. Prefer targeted searches and existing project scripts.
3. Follow the stable first-pass procedure: Search Mobbin with query batches, ingest normalized screen records, verify database counts and Drive upload coverage, and report gaps or rate-limit state.
4. Preserve user changes. Do not discard unrelated dirty work.
5. Verify with the smallest check that proves the requested behavior. Common command/tool signals from history: node, git, npx, sed, rg, ls.
6. Finish with changed files, verification performed, residual risks, and a concrete next step only when useful.

## Evidence To Preserve

- Observed sessions: 4
- Skill score: 46
- Representative prompts:
  - Project: ~/Documents/GitHub/mobbin-library — a TypeScript/SQLite cache for Mobbin UI screens with a Google Drive image sync. What's built: src/drive.ts handles OAuth + image upload to Drive. src/cli.ts has a drive-upload command that uploads images for screens
  - Project: ~/Documents/GitHub/mobbin-library — a TypeScript/SQLite cache for Mobbin UI screens with a Google Drive image sync. What's built: src/drive.ts handles OAuth + image upload to Drive. src/cli.ts has a drive-upload command that uploads images for screens
  - • Handoff instructions for Claude/Gemini: Project: /Users/michael/Documents/GitHub/mobbin-library Goal: Continue pulling popular iOS app screens from Mobbin, ingesting them into SQLite, and uploading images immediately to Google Drive. Current status: - Main D
  - <local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>

## Refinement Notes

After using this skill, update it with exact project conventions, expected output shape, recurring checks, and known failure modes. Keep this file concise; move long examples or schemas into references only if they are repeatedly needed.
